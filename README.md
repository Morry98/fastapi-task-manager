# Fastapi Task Manager
A task manager for Fastapi. Robust Scheduling, Distributed Safety

> [!IMPORTANT]  
> This package is **stable and production-ready**, but the documentation is still being written. 
> We strongly recommend **pinning a specific version** in your dependencies to avoid unexpected breaking changes as the API evolves.
